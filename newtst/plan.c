#include <stdio.h>

int main(void)
{
	//this for thinking
	argv[0] = "ls";  || argv[0] = "/bin/ls"
	//then
	we will use 
	access() || stat() >> to check if the file is there or not

	so we know now tool
	but need to know what is environment

